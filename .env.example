DATABASE_URL="file:./dev.db"

# Only used when manually creating the first administrator with npm run admin:create.
# Do not commit a real password. Clear password variables from the terminal after use.
# Do not keep an administrator's plaintext password in production environment files.
INITIAL_ADMIN_EMPLOYEE_NO=
INITIAL_ADMIN_NAME=
INITIAL_ADMIN_PASSWORD=
